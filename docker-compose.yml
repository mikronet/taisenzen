version: '3.8'

services:
  taisen-zen:
    build: .
    container_name: taisen-zen
    restart: unless-stopped
    ports:
      - "3081:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
